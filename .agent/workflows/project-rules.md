---
description: Project rules for tm_server
---
# Git Commits

**Do NOT automatically add or commit changes without explicit user permission.** Always wait for the user to review changes and give approval before running any `git add` or `git commit` commands.
